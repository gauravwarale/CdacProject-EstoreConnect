
@{
    ViewBag.Title = "AddCategory";
    Layout = "~/Views/shared/Master.cshtml";
}
@model List<EStoreConnect.Models.ProductSubcategory>
<div>

    <div class="table table-bordered" style="display: flex; justify-content: center; align-items: center; width: 50%; margin-left: 300px; background-color: #574d4d; border-color: #574d4d ">
        <form method="post" action="/Product/AddSubCategory">
            <table>
                <tr>
                    <th style="color:white">SubCategory:</th>
                    <th><input type="text" placeholder="Enter SubCategory" name="psubcategory" required /></th>
                </tr>
                <tr style="margin:10px">
                    <th style="color:white">SubCategory Description:</th>
                    <th><textarea cols="25" rows="4" name="subcatdesciption" required></textarea></th>
                </tr>
                <tr>
                    <th colspan="2"><center><button class="btn btn-primary">Add</button></center></th>
                </tr>
            </table>
        </form>
    </div>
    @if (Model!=null)
    {
        <div class="table table-striped">
            <table class="table table-responsive table-striped">
                <thead>
                    <tr>
                        <th>SubCategory</th>
                        <th>SubCategory Description</th>
                        <th>Action</th>
                    </tr>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.psubcategory</td>
                            <td>@item.subcatdesciption</td>
                            <td>
                                <a class="btn btn-primary" href="/Product/EditsubCategory/@item.psubcatId">Edit</a>
                                <a class="btn btn-danger" href="/Product/DeletesubCategory/@item.psubcatId">Delete</a>
                            </td>
                        </tr>
                    }
                </thead>
            </table>
        </div>
    }

</div>


